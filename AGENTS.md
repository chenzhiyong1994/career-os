# Career OS Agent 说明

## 产品定位

Career OS 是一个求职操作系统，当前优先覆盖简历优化、JD 分析和作品集管理；后续也承接与职业表达、项目汇报、作品展示相关的通用工作材料生产，例如演示文档。

优先处理能帮助候选人做出更好投递判断、生成岗位相关简历材料、维护长期个人主页/作品集资产的工作。除非能直接支持某个 `application packet` 或长期个人品牌资产，不要漂移到泛泛的职业建议。
通用工作材料应服务于候选人的职业叙事、项目复盘、对外展示或内部沟通资产，不要扩展成无边界的办公文档代工。

## 核心数据概念

- `candidate profile`：候选人的稳定事实源，包含角色、技能、项目、指标、教育、链接和限制条件。
- `JD scorecard`：结构化岗位分析，包含明确要求、推断优先级、关键词、级别信号和风险/缺口。
- `resume version`：基于已确认事实、面向某个目标 JD 的简历版本。
- `portfolio item`：可复用的公开资产，例如项目、产品、案例研究、文章、社群、演讲、媒体频道、开源贡献、实验或证据链接。
- `content channel`：内容分发或公共触点，例如个人网站、newsletter、博客、播客、视频账号、社交账号、社群或活动系列。
- `application packet`：某个岗位的申请材料集合，包括简历、相关作品证据、备注和后续任务。
- `work material`：可复用的职业表达或项目沟通材料，例如演示文稿、项目汇报、案例展示、讲稿、图文说明或对外介绍。
- `presentation deck`：面向演讲、汇报、教学、项目复盘或作品展示的演示文稿，可作为 `work material` 或 `portfolio item` 的一种输出形态。

## Skill 使用规则

- 当任务需要理解 JD、判断岗位匹配或据此调整内容时，使用 `skills/jd-analysis`。如果简历内容已经确认且用户只要求排版或导出，不要重复做 JD 分析。
- 当任务涉及简历措辞、经历 bullet、ATS 覆盖、岗位相关简历版本时，使用 `skills/resume-tailor`。
- 当任务涉及把已确认的简历内容做成 HTML/PDF、一页 A4 模板、头像/二维码、排版密度、导出或视觉 QA 时，使用 `skills/html-resume-builder`。它只负责成品渲染；纯排版任务直接进入该 skill，不要重新分析 JD 或改写内容。
- 当任务涉及个人主页、作品集分类、项目、内容频道、公开证据、社群/自媒体/演讲/文章等资产时，使用 `skills/portfolio-curator`。不要默认认为作品集必须按单个岗位定制。
- 当任务涉及演示文稿、PPT/PPTX 转换、HTML slides、项目汇报 deck 或演讲材料时，先对全局安装的 `frontend-slides`、`huashu-design`、`guizang-ppt-skill` 做适配建议，再和用户确认使用哪个 skill；不要默认直接开做。

## 守则

- 不要编造候选人的事实、指标、公司、日期、教育、证书或项目结果。
- 每个强表述都要能追溯到用户提供的事实、作品、链接、材料或明确假设。
- 优先输出可结构化沉淀的内容，方便后续变成存储对象。
- 对证据不足和高风险表述要明确标记，不要用自信措辞掩盖。
- 处理公开示例或贡献材料时，按 `docs/privacy-and-redaction.md` 脱敏；真实简历、头像、联系方式和公司内部材料不得进入公开样例。
