# 参与贡献

感谢你愿意帮助 Career OS 变得更可靠。这个项目尤其欢迎能增强事实边界、可审查性、跨平台交付和中文求职语境覆盖的贡献。

## 适合贡献的内容

- 补充 JD 分析、简历优化或作品集整理中的边界案例；
- 改进 Skill 的触发描述、输入输出结构、失败条件和证据要求；
- 增强 HTML/PDF 导出、字体、分页、图片或占位符检查；
- 提出核心数据对象的 Schema、评测集或可解释匹配方法；
- 修正文档、示例、无障碍体验与中英文表达。

## 隐私门禁

不要提交真实简历、头像、手机号、个人邮箱、二维码、招聘人员信息、申请编号、私有 JD 链接、公司内部截图、客户名称、未公开指标或访问凭据。

示例应使用合成数据，并显式标注 `synthetic: true`。即使当事人同意，也优先提交能复现问题的最小虚构案例。完整清单见 [`docs/privacy-and-redaction.md`](./docs/privacy-and-redaction.md)。

如果问题本身涉及仓库已经泄露的敏感信息，请不要创建公开 Issue，改按 [`SECURITY.md`](./SECURITY.md) 私下报告。

## 提交前检查

1. 确认改动只解决一个清晰问题，并保留现有 Skill 的职责边界。
2. 检查新事实是否有来源；无法验证的内容应标记为推断、示例或待确认。
3. 对修改过的 Python 脚本运行语法检查：

   ```bash
   python -m py_compile skills/html-resume-builder/scripts/*.py
   ```

4. 修改模板或导出逻辑时，至少创建一次临时工作区，并运行与改动相关的最窄 QA。
5. 检查暂存区、文件名、图片元数据和 Git 历史，确认没有个人信息、密钥或本地绝对路径。
6. 同步更新与行为变化直接相关的 README、reference 或示例，避免在多个文件重复维护同一份事实。

## Pull Request 建议

请在 PR 中简要说明：

- 解决了什么真实问题；
- 哪些文件或工作流受到影响；
- 如何验证；
- 是否使用合成数据，以及做了哪些隐私检查；
- 是否改变了许可证或引入了第三方材料。

对于较大的 Schema、工作流或产品方向变更，建议先开一个不含敏感材料的 Discussion 或 Issue 对齐范围。
